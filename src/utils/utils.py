def notification_to_admin(message_text):
    """
    The notification_to_admin function sends a notification to the admin.
        Args:
            message_text (str): The text of the message to be sent.

    :param message_text: Pass in the message that needs to be sent
    :return: Nothing
    """
    print(message_text)
    # send_sms(user, message_text)
    # send_telegram(user, message_text)
